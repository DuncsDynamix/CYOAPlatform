# Handover — 2026-08-06 (end of deployment session)

Context for the next working session. Durable state also lives in memory
(`project_demo_deployment.md`, `project_vertical_focus.md`) and the runbook
(`docs/demo-deployment.md`); this note covers the live tail of work.

## Where things stand

- **Deployed and working:** https://traverse-five-lyart.vercel.app (Vercel
  project `traverse-five`, push-to-deploy from `main`). Supabase project
  `jwoaxnewanctucvomwix` (free org, eu-west-2) hosts auth + Postgres; 6
  migrations applied, 4 seeds in, RLS locked. Verified: root 200, login 200,
  `/scenario` gate redirects unauthenticated → login. Duncan's account
  (`74f0dcf6-…`) linked to Gold Tap org.
- **Just shipped (c342d24): the Gold Tap Training Library** at `/scenario` —
  a signed-in learner sees their org's published courses as branded cards
  (title, description, duration, objectives count → cover screen → play).
  Multi-tenant by construction: only the user's org's courses appear.
  Verified locally with Gold Tap branding; deploys with this push.

## Immediate next steps (in order)

1. **CORRECTION + top task — fix the Gold Tap shelf.** "Last Orders"
   (…0050, alcohol service) is an OLD experiment wrongly attached to the
   Gold Tap org — Gold Tap is the WATER-industry trainer (see
   project_vertical_focus memory). The real replicas of Neil's existing
   course are the NWH seeds: …0040 (25-question MCQ certification,
   seed-nwh.ts) and …0041 (situational variant, seed-nwh-interactive.ts).
   Required work, per seed, BEFORE putting them on the deployed shelf —
   they predate this week's standards and possibly earlier context-layer
   changes, so review for completeness and correctness:
   - attach `orgId` (Gold Tap …0051), explicit `status: "published"` +
     `publishedAt` (neither currently sets status — grep found none)
   - `shape.displaySteps` (progress bar), learner-facing `description`
     (now shown on cover + library card), `contextPack.learningObjectives`
     (cover + objectives drawer)
   - checkpoint `marksCompletionOf` = exact objective text if checkpoints
     exist; dialogue nodes (if any) into `assessesNodeIds` + voice casting
   - validate the graph + current `ExperienceContextPack`/option shapes
     against types (tsc passes, but check semantics: option
     `trainingFeedback`, EVALUATIVE rubric presence, endpoint summary)
   - 0042 (slides variant) stays OFF the deployed shelf — its images live
     in public/uploads which is ephemeral on Vercel
   - demote Last Orders: `status: "draft"` on BOTH local and deployed DBs
     (and adjust seed-goldtap.ts so reseeds keep it off the shelf)
   - then seed 0040 + 0041 into the deployed DB (`.deploy-db-url` pattern)
     and verify the library shows: NWH MCQ + NWH situational + Discoloured.
2. **Neil + Kirsty logins** (Duncan does the dashboard half): Supabase →
   Authentication → Add user (email + password, auto-confirm) for each; then
   per user: `DATABASE_URL="$(cat .deploy-db-url)" DIRECT_URL="$(cat .deploy-db-url)" npx tsx prisma/link-demo-user.ts <uuid> <email> "<Name>"`.
   Share credentials + the `/scenario` library link (not a bare domain —
   root is the fiction library). Separate accounts per person is deliberate:
   competence records must be attributable.
3. **ElevenLabs**: sign up → `ELEVENLABS_API_KEY` into Vercel (Prod+Preview)
   → redeploy. Deployed dialogues are silent until then.
4. **Library growth**: the pitch is "your existing courses replicated + the
   new scenario kind". Next candidates from Gold Tap's catalogue for
   scenario treatment: doorstep refusal-of-entry, emergency supply, sampling
   under pressure (see docs/goldtap-discovery-questions.md §1). New courses
   = new seeds attached to org …0051; reseed deployed DB with the
   `.deploy-db-url` pattern.

## Open items / gotchas carried forward

- Local dev DB's 0050 is stale (title/status from an old seed) — harmless;
  fix by deleting the row locally and re-running seed-goldtap if it annoys.
- Supabase free tier pauses after ~1 week idle — wake before demos
  (demo-day checklist in docs/demo-deployment.md).
- Old Vercel project `traverse-mauve` may still exist — delete if listed.
- `.deploy-db-url` (gitignored, repo root) holds the session-pooler string
  for laptop-side DB ops; migrate needs DIRECT_URL set too.
- Verify step 7 (RLS anon curl returns `[]`) not yet run against the
  deployed project — 30 seconds, needs the publishable key.
- ESLint 8→9 migration parked (`.npmrc` legacy-peer-deps carries it).

## The demo flow as it now exists

Login → Gold Tap Training Library (`/scenario`) → pick course → branded
cover screen (objectives, assessment disclosure) → play (voice once key
added; wrap-up-early; shuffled options; live progress + objectives ticks) →
Evidence Record at debrief (print/PDF) → full session record at
`/api/v1/engine/record?sessionId=`. Discovery questions for the meeting:
docs/goldtap-discovery-questions.md — demo section leads with Discoloured.
